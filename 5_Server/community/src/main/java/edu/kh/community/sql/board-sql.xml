<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<comment>board-sql.xml</comment>

	<entry key="selectBoardName">
		SELECT BOARD_NM FROM BOARD_TYPE WHERE BOARD_CD = ?
	</entry>
	
	<entry key="getListCount">
		SELECT COUNT(*) FROM BOARD WHERE BOARD_CD = ? AND BOARD_ST= 'N'
	</entry>
	
	<entry key="selectBoardList">
		SELECT * FROM (

SELECT ROWNUM RNUM, A.* FROM (

    SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, MEMBER_NICK, TO_CHAR(CREATE_DT, 'YYYY-MM-DD') AS CREATE_DT,READ_COUNT FROM BOARD JOIN MEMBER USING(MEMBER_NO) WHERE BOARD_CD= ? AND BOARD_ST ='N' ORDER BY BOARD_NO DESC
)A

)
WHERE RNUM BETWEEN ? AND ?
	</entry>
	
</properties>
